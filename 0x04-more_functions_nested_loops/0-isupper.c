#include "main.h"
/**
 * _isupper - checks for uppercase character.
 *@alphabet: print int
 * Return: 0 or 1.
 */

int _isupper(int alphabet)
{
if (alphabet >= 'A' && alphabet <= 'Z')
{
return (1);
}

else
{
return (0);
}

}
